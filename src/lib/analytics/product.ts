/**
 * Vantage product name — the ONE place the display name lives (build spec
 * Appendix B). It appears only in the admin nav label + the privacy policy.
 *
 * Deliberately NOT used to derive the beacon path: that stays the neutral /p/e
 * for ad-blocker resilience, regardless of what we call the product.
 */
export const ANALYTICS_PRODUCT_NAME = "Vantage";
